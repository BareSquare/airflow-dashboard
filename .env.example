# Copy this file to .env and provide the Airflow API details before running locally.
# Never commit populated .env files.

AIRFLOW_API_BASE_URL=https://your-workspace.astronomer.run
AIRFLOW_API_VERSION=2
AIRFLOW_API_TOKEN=replace-with-read-only-token
